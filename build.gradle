apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

archivesBaseName = 'gradle-witness'
version = '1.0.0'
